<?xml version="1.0" encoding="utf-8"?>
<local:Screen
    x:Class="EasyVhd.Views.AttachVhdScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EasyVhd.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="1.333*" />
            <RowDefinition Height="4.666*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="0.5*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*" />
            <ColumnDefinition Width="6.5*" />
            <ColumnDefinition Width="2.5*" />
            <ColumnDefinition Width="0.5*" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="1" Grid.Column="1" FontSize="24px" VerticalAlignment="Center">VHD 연결</TextBlock>
        <StackPanel Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Orientation="Vertical">
            <TextBlock FontWeight="Bold">VHD 경로</TextBlock>
            <StackPanel Margin="0,10,0,0" Orientation="Horizontal">
                <TextBox Width="700px" IsEnabled="{x:Bind vm.ControlsEnabled, Mode=OneWay}" Text="{x:Bind vm.VhdPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <Button Margin="10,0,0,0" IsEnabled="{x:Bind vm.ControlsEnabled, Mode=OneWay}" Command="{x:Bind vm.OpenVhdPathBrowseCommand}">찾아보기...</Button>
            </StackPanel>
        </StackPanel>
        <CheckBox Grid.Row="3" Grid.Column="1" VerticalAlignment="Top" IsEnabled="{x:Bind vm.ControlsEnabled, Mode=OneWay}" IsChecked="{x:Bind vm.ReadOnly, Mode=TwoWay}">읽기 전용</CheckBox>
        <ProgressRing Grid.Row="4" Grid.Column="2" IsActive="{x:Bind vm.ProgressActive, Mode=OneWay}" />
        <StackPanel Grid.Row="5" Grid.Column="2" Orientation="Horizontal">
            <Button Width="100px" Margin="11.5,0,0,0" IsEnabled="{x:Bind vm.ControlsEnabled, Mode=OneWay}" Command="{x:Bind vm.PreviousCommand}">이전</Button>
            <Button Style="{StaticResource AccentButtonStyle}" Width="100px" Margin="11.5,0,0,0" IsEnabled="{x:Bind vm.ControlsEnabled, Mode=OneWay}" Command="{x:Bind vm.AttachVhdFileCommand}" CommandParameter="{x:Bind}">연결</Button>
        </StackPanel>
    </Grid>
</local:Screen>
