<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="EasyVhd.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EasyVhd.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="EasyVhd">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <NavigationView IsBackButtonVisible="Collapsed" SelectionChanged="{x:Bind vm.NV}">
            <NavigationView.MenuItems>
                <NavigationViewItem Icon="Home" Content="홈" IsSelected="true" Tag="Home" />
                <NavigationViewItem Content="백업 및 복원" Tag="BackupRestore">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEDA2;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Icon="Repair" Content="도구" Tag="Tools" />
            </NavigationView.MenuItems>
            <UserControl Content="{x:Bind vm.CurrentScreen, Mode=TwoWay}" />
        </NavigationView>
    </Grid>
</Window>
