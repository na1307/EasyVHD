<?xml version="1.0" encoding="utf-8"?>
<local:Screen
    x:Class="EasyVhd.Views.HomeScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EasyVhd.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="8*" />
            <RowDefinition Height="0.5*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*" />
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="0.5*" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="3" HorizontalAlignment="Center" Text="{x:Bind vm.SelectedVhdText, Mode=OneWay}" />
        <Button Width="250px" Height="50px" Grid.Row="2" Grid.Column="1" Command="{x:Bind vm.SelectVhdCommand}" CommandParameter="{x:Bind}">VHD 선택</Button>
        <Button Width="250px" Height="50px" Grid.Row="2" Grid.Column="2" Command="{x:Bind vm.CreateVhdCommand}" CommandParameter="{x:Bind}">VHD 만들기</Button>
        <Button Width="250px" Height="50px" Grid.Row="2" Grid.Column="3" Command="{x:Bind vm.DeselectVhdCommand}" CommandParameter="{x:Bind}">VHD 선택 해제</Button>
    </Grid>
</local:Screen>
